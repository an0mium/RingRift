/**
 * Archived: ClientSandboxEngine.aiStall.seed1 has moved to archive/tests/unit.
 *
 * This file now serves as a no-op stub. The original seed=1 stall regression
 * harness lives under archive/tests/unit/ClientSandboxEngine.aiStall.seed1.test.ts
 * and is intended for opt-in diagnostics only.
 */

describe('ClientSandboxEngine.aiStall.seed1 (archived)', () => {
  it('is archived under archive/tests/unit/ClientSandboxEngine.aiStall.seed1.test.ts', () => {
    expect(true).toBe(true);
  });
});

# RingRift Cluster Node Configuration - EXAMPLE TEMPLATE
# Copy this file to cluster_nodes.env and fill in your actual node IPs.
#
# The actual cluster_nodes.env is gitignored to protect private IPs.
# Used by cluster_health_check.sh and cluster_monitoring.sh

# Primary training node (2x H100 80GB)
# H100_IP="10.0.0.1"  # Replace with actual Tailscale IP

# GH200 nodes (96GB unified memory each)
# GH200_NODES=(
#     "10.0.0.2:gh200e"
#     "10.0.0.3:gh200f"
#     "10.0.0.4:gh200g"
#     "10.0.0.5:gh200b"
# )

# A10 node (for gauntlet/evaluation)
# A10_IP="10.0.0.10"

# CPU-rich nodes (for CMA-ES and data processing)
# CPU_NODES=(
#     "cpu1.example.com:aws-staging"
#     "10.0.1.1:hetzner-cpu1"
# )

# Example with filled values (replace with your actual IPs):
# H100_IP="100.x.x.x"
# A10_IP="100.x.x.x"
# GH200_NODES=(
#     "100.x.x.x:gh200e"
#     "100.x.x.x:gh200f"
# )

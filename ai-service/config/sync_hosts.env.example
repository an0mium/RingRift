# RingRift Sync Hosts Configuration - EXAMPLE
# Copy this to sync_hosts.env and fill in your actual host details
# Used by scripts/sync_training_data_cron.sh and scripts/sync_all_nodes.sh

# Primary host for data sync (required for sync_training_data_cron.sh)
# Format: user@host or user@ip
SYNC_PRIMARY_HOST=ubuntu@gpu-primary

# Fallback host (optional - leave empty if not needed)
SYNC_FALLBACK_HOST=ubuntu@gpu-secondary

# Coordinator configuration (for sync_all_nodes.sh)
# Replace with your coordinator's Tailscale IP
COORDINATOR_IP="10.0.0.1"
COORDINATOR_PATH="/workspace/ringrift/ai-service/data/games"

# Nodes to sync from (space-separated, format: "name:ip:path")
# Example with multiple nodes:
NODES_STRING="node1:10.0.0.2:/root/ringrift/ai-service/data/games node2:10.0.0.3:/root/ringrift/ai-service/data/games"

# Individual node examples:
# - Vast.ai instance: "vast-rtx3070:100.x.x.x:/root/ringrift/ai-service/data/games"
# - RunPod GPU pod: "runpod-h100:100.x.x.x:/workspace/ringrift/ai-service/data/games"
# - Local Mac: "mac-mini:10.0.0.5:/Users/user/ringrift/ai-service/data/games"

/**
 * Archived sandbox AI stall regression guard for seed=1.
 *
 * This suite has been moved under archive/tests/unit so that it no
 * longer participates in normal Jest profiles. It is retained as a
 * targeted repro harness for historical AI stall behaviour on
 * square8 / 2 AI / seed=1.
 *
 * To rerun this diagnostic locally, invoke Jest directly against this
 * file with RINGRIFT_ENABLE_SANDBOX_AI_STALL_REPRO=1 set in the
 * environment.
 */

export {};

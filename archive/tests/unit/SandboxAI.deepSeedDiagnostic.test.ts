/**
 * Archived deep-seed AI stall diagnostic (sandbox vs backend).
 *
 * This suite has been moved out of the main tests/unit tree to avoid
 * impacting normal Jest runs. It is preserved here for historical
 * debugging of sandbox AI stall behaviour on specific seeds.
 *
 * To rerun this diagnostic locally, invoke Jest directly against this
 * file (usually with increased heap / NODE_OPTIONS set as needed).
 */

export {};
